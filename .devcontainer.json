{
  "name": "operations-research-two-tsp",
  "image": "mcr.microsoft.com/devcontainers/base:debian",
  "features": {
    "ghcr.io/msclock/features/vcpkg:2": {
      "username": "vscode",
      "vcpkgversion": "latest"
    },
    "ghcr.io/devcontainers/features/python:1": {
      "pythonVersion": "3.11",
      "installTools": true
    }
  },
  "extensions": [
    "ms-vscode.cmake-tools",
    "ms-vscode.cpptools",
    "ms-python.python",
    "ms-python.vscode-pylance"
  ],
  "postCreateCommand": "if [ -f requirements.txt ]; then python3 -m pip install --upgrade pip && pip3 install -r requirements.txt; else echo 'No requirements.txt found, skipping pip install.'; fi && cmake --version && python3 --version",
  "remoteUser": "vscode",
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=operations-research-two-tsp,target=/workspace,type=volume,consistency=cached"
}
